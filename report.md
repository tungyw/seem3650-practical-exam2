## Step 2

ANTIGONUS:

There, he son, but

HASTINGS:

A gentleman's leave hands: and him us him to back:

And and swear my facest to more heavens


## Step 3 report validation loss + best settings.md


-Heads=2 → val loss=1.6439

-Heads=3 → val loss=1.6245

-Heads=5 → val loss=1.6084

-Heads=7 → val loss=1.5966

**Lowest validation loss:** 1.5966 achieved with **layers=5, heads=7**.
## Step 4 – Code generation (C/C++)

Token count after prepare.py: 12,156,695

First 20 lines of generated samples:

 if( pOnly->eStat==OP_State==OP_GitState==OP_Rowid ){
       return pOnlyno->eState;
    }
  }

  /* The delimit. */
  for(i=0; ii<0; ii<p->pGrowid; i++)
     if( p->iRowid ){
     return p->iRowid && p->u.pNew;
  }
  if( p->rc==SQLITE_OK ){
    for(i=0; p->pParse->nRowid = sqlite3OsFile(p->pParse, p->pRowid);
    if( p->aDelt!=SQLITE_OK ){
     return SQLITE_OK;
  }

  if( p->pRet==SQLITE_OK && !p->pNew==SQLITE_OK ){
    p->pOff = aFile[i];
     p->aOff[i] = SQLITE_OK;
  }

  ## Step 4 – favorite generated snippet

  static void while(
  void *pReader,                      /* data */
  void *pLeft,                            /* Found has */
  void);
